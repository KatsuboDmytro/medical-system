import { createSlice } from "@reduxjs/toolkit";

const soldiersSlice = createSlice({
  name: "soldiers",
  initialState: {
    soldiers: [],
  },
  reducers: {

  }
});

export default soldiersSlice;
