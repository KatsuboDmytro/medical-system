import { createSlice } from "@reduxjs/toolkit";

const humanSlice = createSlice({
    name: "human",
    initialState: {
        human: [],
    },
    reducers: {

    }
});
export default humanSlice;
